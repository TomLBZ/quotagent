# 交接（每轮次更新，≤ 1024 B）

<!-- budget: 1024 bytes, hard -->

## 现在在哪

**P2 进行中**：9 件自进化产出插件；AI agent 决策建议层（`advice-panel` 30/30）。
本轮收口 **D-071**（EV-152 / 决策 D-072）：文档门扫描范围 = **契约文档集合**（排除临时/派生目录；267 → 98），与整树副本门解耦；真契约文档在读窗口里消失**仍判红**（不跳过）；`AC-RUNTIME-001` 那条断言改为「**契约文档集合**不变」+ 门自报数对账。
实况：并发 12/12 绿（旧门 11/12 红）；`p0-no-node` 并发 59/59；九道门全绿。

## 下一步唯一动作

`progress-checklist.md` 未完成行；缺口 FR 见 `docs/design/15-requirements-coverage.md` §3。

## 不变量与阻塞

内核不可自改 · 账本唯一写者 · **账本行永不销毁** · commit 面不进桥 · 私域不出 realm · V 只能人签。
待人手：**T-224 需把 Jev key 放进 `/workspace/config.yaml`**；G0/G1 签署。无技术阻塞。

# 交接（每轮次更新，≤ 1024 B）

<!-- budget: 1024 bytes, hard -->

## 现在在哪

**P1 进行中**；已完成任务见 `progress-checklist.md` 的 done 行。最近一批
**T-214（比较表导出）done**：逐行导出（含被排除报价及其原因与下一步）、与 `Evaluation` **及账本**逐行核对、
引用链全部可解析（篡改即失败并定位）、含 Flag 与差异说明、字节确定性、CSV 带 BOM。严格引用解析还抓到并修掉了
`compare.tco` 的**悬空引用 `ledger:0`**。八入口全绿；**56 条 AC 全绿**。

## 下一步唯一动作

**B16 = T-215**：部署与操作手册（一页能跑起来）+ `tools/verify.sh g1` 门 → **P1/MVP 收口**。

## 不变量

内核不可自改 · 账本唯一写者 · commit 面不进桥 · 私域不出 realm · V 结论只能人签（详见 AGENTS.md）。

## 阻塞

无。V 项真实结论仍待人工。

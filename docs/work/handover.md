# 交接

<!-- budget: 1024 bytes, hard -->
## 现在在哪

P2。本批（EV-177 / T-327）：① `host/lib/**` **13 个**进归属插件 `code/`（薄重导；**先改读方 6 处**）
② **12 个**自进化产物**追链搬迁**（日志同步路径+sha256；可复跑校验反向 4/4 必红）③ `tools/**` 非薄入口
**12 项**进各自 `tests/`（薄转发）④ **12 个**插件补真实承载。`plugin-assets` 已搬 **108**、非薄入口 **30**
（基线收紧）；插件入口 **28**，余 35 标 `code:`。

## 下一步唯一动作

续搬余下 **30 项 `tools/**`**、`host/modules/**` 剩 16 个非自进化实体、`host/lib/**` 剩 2 个（都 `import
'cordis'` ⇒ 先解决裸解析）。规则见映射表 §规则。

## 不变量

内核不可自改·账本唯一写者·账本行永不销毁·commit 面不进桥·私域不出 realm·V 只能人签·**门名是接口**。
`run-clone`/`clean-copy` 校验 HEAD ⇒ 提交后跑。待人手：T-224 Jev key；G0/G1 签署。

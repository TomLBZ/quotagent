# 交接（每轮次更新，≤ 1024 B）

<!-- budget: 1024 bytes, hard -->

## 现在在哪

**P1 进行中**：T-201、T-216、T-217、T-202、T-218、T-203、T-204、T-205、T-206+T-207、T-208 与
**T-210（人工门队列 + 超时）done**：队列视图含动作/摘要/引用链/Flag/置信度/超时策略/等待时长；
超时三选一（`remind`/`escalate`/`abort`）且**从入口上就没有「自动批准」**；`escalate` 必须给 human 上级；
待批**不阻塞**其他工作（只有该动作被 `require` 挡下）。`verify.sh` 七个入口全绿；**50 条 AC 与 s1..s4 全绿**。

## 下一步唯一动作

**B12 = T-211**：条款库与冲突标注（AC-TERMS-001 / FR-TERMS-001/002、FR-GUARD-004）。
拆解见 state.json 的 `next_action`。

## 不变量

内核不可自改 · 账本唯一写者 · commit 面不进桥 · 私域不出 realm · V 结论只能人签（详见 AGENTS.md）。

## 阻塞

无。V 项真实结论仍待人工。

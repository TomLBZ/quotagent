# 交接

<!-- budget: 1024 bytes, hard -->
## 现在在哪

P2。本批（EV-175 / T-324）：① `AC-COMPARE-004` 改读**决策定义集合**（主文件 + 归档）⇒ 绿；
反向验证（抽走 `D-016`）必红 ② **阶段 5 第三小片**：18 个服务实体进 `code/`（守恒/两半边 18/18）
⇒ 服务 **30/30 搬完**；**先改读方再搬** 9 处 ③ **阶段 4.2 续搬** 10 项 `tools/**` 进 `tests/`（逐门计数不变）。`plugin-assets` 已搬 **84**、非薄入口 **54**（64→54）。

## 下一步唯一动作

照 `plans/plugin-migration-plan.md` §2 续搬**阶段 4.2 余下 54 项 `tools/**`**（登记 + `git mv` + 薄转发；
门名与 `verify.sh` 分支一行不改）；再收剩余实体。

## 不变量

内核不可自改 · 账本唯一写者 · 账本行永不销毁 · commit 面不进桥 · 私域不出 realm · V 只能人签 · **门名是接口**。
`run-clone`/`clean-copy` 校验 HEAD ⇒ commit 后跑。待人手：T-224 Jev key；G0/G1 签署。

# Generated content identity and sharing
<!-- budget: 2048 bytes, hard -->

2026-09-26. `node src/system/plugin-studio/tests/evolution.mjs` →
[15 native groups passed](native.json), using real Cordis, Python ledger and immutable
artifact files. Model output is a declared fixture, not live-provider evidence.

New assertions: generation stores a64-character prompt digest; identical generated
content returns the same account plugin ID with no additional lifecycle events;
repeating after explicit disablement retains disabled state. Empty feedback and
inventory reads append nothing. A deliberately changed active artifact is visible
as inconsistent, and publish/install/global promotion all refuse it. A stale GUI
revision reference also refuses. Existing independent-copy, paired execution,
rollback, management-unload and restart checks remain passing.

`node src/system/plugin-studio/tools/lineage-check.mjs` also passed active fallback,
configuration remount, deterministic duplicate retirement, concurrent loads and
idempotent marketplace install. Full React build passed110 modules. Prior actual-model
public generation/evolution evidence remains in `../integrated-public-2026-09-26/`;
this focused check does not claim a new public deployment or a deterministic model.

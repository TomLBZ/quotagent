EV-065 canary 接线到真实入口（T-230）
生成时间：2026-09-21T08:49:26Z（UTC）

约束：`ADR-0017 §4` 的已知限制（"真实网络分流尚未接线"）→ 本批次把它接上；用户 2026-09-21 指令
"每个功能都由插件提供"。以下为原始命令输出。

[1] tools/verify.sh canary-route（分流接线 + 失败隔离 + 样本回灌 + UI 路径端到端，8 条断言）
[ok]   weight=0 → 全部走 base，输出与未接线时字节一致（升级路径默认零影响）
        lanes=["base","base","base","base","base"]
[ok]   分桶确定性：同一 key 每次调用落同一条道（真实调用路径上可复现）
        两次=["base","base","canary","canary","canary","canary"]
[ok]   负控→正控：候选实现抛错时**回退 base**（调用方不受影响），且 canary 侧如实记 ok=false（错误不被吞掉）
        lane=canary fallback=true canary 错误数=1
[ok]   负控：base 抛错必须**原样抛出**（不得静默回退掩盖真实故障）
        error=base-boom
[ok]   样本回灌：真实调用驱动判定，且样本数 = 投递数 + 回退数（回退时 base 侧也如实记一条）
        base=25 canary=28 投递=40 回退=13
[ok]   候选退化 → decide() 给出 **automatic 回滚**（安全动作，不需要人工批准）
        action=rollback reasons=["错误率 0 → 4643 bp（阈值 +200）"]
[ok]   端到端正控：weight=0 → UI 供应商视角走 base 投影（响应里没有候选标记，接线默认零影响）
        lane=base 含 quote_id=true 含候选标记=false
[ok]   端到端正控：weight=10000 → 同一路由走**候选投影**（响应里出现候选标记）；样本回灌给 canary
        lane=canary 含候选标记=true 样本 base=0 canary=1
RESULT: PASS（canary-route 门 8/8）
（exit=0）

[2] tools/verify.sh webui（投影改成独立插件后，UI 的 HTTP 级检查）
tor/` 与 `/quotagent/supplier/`）
        contractor=200 supplier=200
[ok]   WebUI 正控：两视角看到的事件集合不同（承包商含 compare/*，供应商不含）
        contractor=5 条 supplier=4 条
[ok]   私域**负控**：供应商视角看不到承包商私域键（`cost_floor`/`markup_pct`/`calendar:private`）
        泄漏=[]
[ok]   私域**非空转对照**：同一批私域数据在承包商视角**可见**（证明确实在源里，供应商看不到不是因为"什么都没有"）
        承包商视角可见=["cost_floor","markup_pct"]；供应商视角含 cost_floor=false
[ok]   私域负控：投影层把"可见类型但含私域键"的行标为 suppressed，且**对外原因通用**（键名不外泄）
        suppressed=1 行；对外原因=private-field-suppressed；审计（仅服务端）=cost_floor
[ok]   私域对照：同一行的私域内容在承包商投影里保留（两侧规则不同，不是一刀切）
        contractor suppressed=0
[ok]   健壮性负控：账本含 null 字段时服务**不崩**（请求级兜底），随后 /api/health 仍可用
        视图 status=200（<!doctype html><html lang="zh"><head><me…）健康 status=200
[ok]   WebUI 负控：未知路径/视角返回 404 且带可用路径提示（不得静默空页面）
        status=404
[ok]   零残留：dispose 后 HTTP 服务关闭且**端口释放**（可被重新监听）
        port=37611 可重监听=true
RESULT: PASS（webui 门 11/11）
（exit=0）

[3] tools/verify.sh modules（进树模块 fixture A1..A6；含新插件 projection）
  const 键：compare.deterministic, kernel-bridge.credit_window, norm.enforce_tolerance, sourcing.deterministic, timeline.deterministic
------------------------------------------------------------------------
模块：9 个（canary, compare, kernel-bridge, norm, projection, sourcing, timeline, webui, __set__）；事件表 91 条（真源：Python 侧）
fixture 断言：105/105 通过；报告落 /workspace/projects/quotagent/tmp/modules-report.json
（exit=0）

[4] 线上服务回读（改完宿主代码后**真的重启**再回读；门全绿不等于服务能起）
$ /workspace/bin/ws-gateway start quotagent && sleep 35 && curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8093/quotagent/api/health
200（/quotagent/contractor/ 与 /quotagent/supplier/ 亦为 200）

结论：
- 投影独立成插件 `projection`（功能归属），`webui` 只做 HTTP 与路由；
- canary 分桶**真的**决定 UI 请求走 base 还是候选投影：weight=0 → base（响应无候选标记）；
  weight=10000 → canary（响应出现候选标记），且**一次请求恰好回灌 1 条样本**；
- 失败隔离：候选抛错 → 回退 base 且如实记 ok=false；base 抛错 → 原样抛出（负控）；
- 实测修掉两个真问题：UI 一次请求投影两次（采样翻倍）、`cli.mjs` 挂载漏 `projection`（服务起不来）。

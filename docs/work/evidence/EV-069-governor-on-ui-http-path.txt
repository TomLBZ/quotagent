EV-069 governor 接进 UI 真实 HTTP 路径（T-234，完成）
生成时间：2026-09-21T09:16:17Z（UTC）

根因链（详见 D-027）：① 模块用 `ctx.governor` **自引用**自己 → 包装挂载下请求期被 ctx 代理拒绝（全路由 500）；
② `cli.mjs` 的 webui 挂载包装漏了 `governor` → 线上服务起不来；③ 检查器占额度的 key 与应用侧 key 不同桶
（`webui:/api/health` vs `webui:/quotagent/api/health`）→ 429 一度不触发。三处均已修。

[1] tools/verify.sh webui → **14/14 PASS**（原始输出见提交附带的 /tmp 或本地复跑）
    含：背压端到端（429 + Retry-After）、归还后恢复 200、错误映射三档单测（429/504/500）
[2] tools/verify.sh governor → 9/9 PASS（语义层：准入/背压/超时/有界重试）
[3] tools/verify.sh modules → 144/144 PASS；docs / plugins / events → exit=0
[4] 线上：`ws-gateway stop+start` 后 /quotagent/api/health、/quotagent/contractor/、/quotagent/supplier/ → 200；
    公网 https://novara.remoteblossom.com/quotagent/api/health → 200

结论：`governor` 已在 UI 真实请求路径上生效（背压可解释地返回 429 + Retry-After），三档错误映射均有断言；
线上服务健康。

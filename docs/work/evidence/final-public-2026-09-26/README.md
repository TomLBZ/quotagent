# Public requirement-completion release
<!-- budget: 6144 bytes, hard -->

URL: https://novara.remoteblossom.com/quotagent/ . Final application source:
`67bd9af5b73036104f81abdacae0883fd8d2ffce`. [Release record](release.json)
identifies the immutable committed archive, data backup and running process.
Build:114 modules,35 native composition plugins. [Copied-data preflight](preflight.json)
passed36 reads across contractor, supplier and administrator accounts before swap.

[Executed commands and per-run revisions](runs.json) link the retained JSON reports.
Twenty completed browser journeys passed97 groups with0 JavaScript errors.
Initial successful journeys ran on `bf2cb4b`; affected Orders and saved-view paths
were then replayed on corrected `67bd9af`. Earlier reports keep their actual revision.
All business mutations came from visible GUI controls. API reads and captured
responses were used to verify outcomes, never to seed a public success.

| Public journey | Observed behavior |
|---|---|
| Dashboard, styles and Help | Actual Projects & routes entry, account sign-in, persistent agent-first layout, role-specific walkthroughs and current feature Help |
| Scope and quotations | Authored scope/schedule, firm/indicative declarations, alternative/additional lines retained into independently approved signed PO, version-bound FAQ reuse |
| Batch submissions | Individual frozen reviews; real partial execution; retry only the failed changed offer; readable business previews |
| Terms and negotiation | Exact unit conversion, source-bound term exceptions, private price floor, separately reviewed negotiation and quote submission |
| Responses and measures | Current/stale response distinction, account-readable source evidence and descriptive baseline/CSV; unknown quality and missing costs remain unknown |
| Exchange and email | Actual signed packages/receipts, gap recovery, reviewed SMTP transfer, explicit IMAP import and separate reported bounce |
| Attachments and retention | Selected counterparties, text/PNG/PDF rendering, immutable versions, active reference/hold constraints and exact reviewed fixture-byte disposal |
| Model evidence | Real configured provider call and matched exact dispatch-request reconstruction; independently trusted signed bundle verification |
| Settings and activity | Masked YAML preview/apply/export,27 real IMAP notifications, pagination/mute/read-all and one explicitly reviewed capped SMTP digest |
| Operations and views | Administrator metadata, persisted30→45→30 interval, personal search/sort/save/reload without lost input |
| Imported sources | Missing-value questions, labelled authored assumptions and exact-value human confirmation before private draft creation |
| Offline app | Chromium installability, current static assets only, visible failed offline write with no replay, reconnection and cache removal |
| Isolated practice | Eight native scenario steps, separate supplier costs, reset/resume/exit/clear and narrow-screen boundary |

Examples: [workspace](agent-workspace.png), [signed scope and order](signed-order.png),
[batch review](batch-review.png), [digest review](digest-review.png),
[sourced measures](business-measures.png), [mobile practice](mobile-demo.png).

The initial public run exposed an [Orders display defect](../public-award-view-fix-2026-09-26/README.md)
and a [saved-preference loading race](../collection-readiness-2026-09-26/README.md).
Both have separate fixes, retained failures and successful corrected public replays.
Harness-only accessible-name/asynchronous-render corrections and their failed
attempts remain in the raw directories named by the reports.

Protocol fixtures bind loopback only and do not forward external messages. SMTP
acceptance is distinguished from signed QEP delivery. Real provider calls are
labelled separately from protocol fixtures. Browser installability is not an
actual operating-system installation; measurements are observations, not service
guarantees or invented savings. Earlier public agent-control/usage and team/runtime
journeys retain their revision-specific evidence in the linked catalog.

The [generated-plugin journey](evolution/combined.json) used two actual configured
model calls: a generated utility returned6, its proposed revision returned9, and
reviewed trial/rollback/restore all executed. GUI publication created an independent
supplier copy. Both owner and copy continued to run while Studio was disabled;
Studio was restored. This journey completed across an initial session and a resumed
session: the first had5 completed groups before an immediate resize assertion;
unchanged-product reproduction and the resume passed, with0 browser errors. The
original failure is retained, not represented as a clean first run. Inspect
[paired outputs](evolution/03-paired-results-five-checks.png),
[actual rollback](evolution/04-observed-trial-rollback.png) and
[independent copy without Studio](evolution/resume/09-independent-copy-without-studio.png).

The independent preference evaluation is recorded separately before requirement
closure.

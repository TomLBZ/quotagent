# 证据索引（P2 product：EV-153 起的本批证据）

> P0 阶段的证据在 [`INDEX.md`](INDEX.md)：EV-001..EV-038；P1 及之后在 [`INDEX-P1.md`](INDEX-P1.md)：EV-039 起。
> 本页只收 **P2 product 批次**的证据行（`INDEX.md` 已到 8076/8192 B 预算上限，无法再追加；分册与 `INDEX-P1.md` 同形）。
> 每份证据的原始命令输出在 `EV-<编号>-*.txt`。

| 证据 | 内容 | 关联 | 阶段 |
|---|---|---|---|
| EV-156 | **「授权区间」**（`authority-band`，正面回答 P-12「谈判让步的授权区间不可见」：**谁能批到多少 / 越界怎么办 / 下一个能批的人是谁**）：围栏门 **22/22**（**三例边界值手算对账**：恰等于限额 500000/500000 ⇒ 在区间内、越界 0；超一分 500001 ⇒ 越界 1 分、要求角色升到 lead、出升级命令；差一分 499999 ⇒ 在区间内；**未配置不得编限额**：角色没登记/`null`/快照缺失/单位非 `cents` ⇒ `unconfigured=true` + `required_role`/`next_role` 空，`null`≠`0`；**越界必出可复制升级命令**且门名真在 `verify.sh help` 里；插件**不能批准**；金额非法三类拒绝；确定性/有界/私域哨兵 0 命中/零写面/四道页面子导航入口/页面 0 内联脚本；**4 处单点变异全红** + 防假变异 + 还原字节一致）+ 真路由门 **12/12**（真进程真回读：两视角页面与 JSON 200、三例与手算一致、**改配置前后同一金额结论不同**（`--config-file` 临时配置；真 `config.yaml` 指纹前后一致）、未配置 ⇒ 角色字段空、升级命令真存在、私域哨兵 0 命中、只读⇒账本零新增、0 内联脚本） | AC-AUTH-001 / T-284 | P2 |

# 证据索引（P2 product：EV-153 起的本批证据）

> P0 阶段的证据在 [`INDEX.md`](INDEX.md)：EV-001..EV-038；P1 及之后在 [`INDEX-P1.md`](INDEX-P1.md)：EV-039 起。
> 本页只收 **P2 product 批次**的证据行（`INDEX.md` 已到 8076/8192 B 预算上限，无法再追加；分册与 `INDEX-P1.md` 同形）。
> 每份证据的原始命令输出在 `EV-<编号>-*.txt`。

| 证据 | 内容 | 关联 | 阶段 |
|---|---|---|---|
| EV-175 | **搬迁收口批**（`T-324`）：① 修 `AC-COMPARE-004`（P1，搬前即红）：断言改读**决策定义集合**（主文件 + 同目录 `decisions-archive*.md`），判据**未放宽**；反向验证（归档抽走 `D-016`）⇒ 同一条断言必红，归档复原 sha256 逐字节一致 ② **阶段 5 第三小片**：余下 **18 个**服务实体进 `src/<层>/<插件>/code/`（`git show HEAD:` blob **守恒 18/18**；旧路径**薄重导**；两半边 **18/18**：旧导入路径可用 + `co_filename` 指实体 + HEAD 顶层定义名逐名在场）⇒ 服务 **30/30 搬完**；**先改读方再搬 9 处**（实测：`p0-no-node` 的 AC-MAIL-002 被 `checks_mail_transport` 读红过）；反向验证 **7/7**（实体变异必红 + 重导变异仍绿 + 逐字节复原） ③ **阶段 4.2 续搬 10 项** `tools/**` 非薄入口进各自 `tests/`（旧位置薄转发 ≤ 目标 1/4；逐门 rc 与 passed/total **逐项不变**）④ `plugin-assets`：RELOCATED 74→**84**、`BASELINE_NONTHIN` **64→54**（**收紧**）；17 门全绿。原文见 `EV-175-batch5-raw.json` | AC-COMPARE-004 / AC-PLUGIN-004 / `T-324` | P2 |
| EV-174 | **迁移终批：25 个 `qa` 检查 + 阶段 5 第二小片 12 服务 + 修 `storage` 遗留红**（`T-323`）：① 剩 **25 个** `src/quotagent/qa/checks_*.py` 搬进各自插件 `tests/`（至此 **47/47**；旧位置 `importlib` 薄转发，`qa` 包导入面/AC 注册/`verify.sh ac` 一字未改；**85 条已注册 AC 的断言名与 ok 逐条对拍全同**）② **12 个**服务模块实体进 `src/<层>/<插件>/code/`（`git show HEAD:` blob **逐字节守恒 12/12**；旧路径**薄重导**（在本模块命名空间里 `exec`）⇒ 相对导入一字不改；实测旧导入路径可用且 `co_filename` 指向实体）③ **修 `storage` 遗留红**：事实区判据在 `user-space` 为符号链接时失效（`_inside(USER_SPACE, <realpath>)` 恒假 ⇒ 「别人的 ns」整类拒绝失效；修前实测 **`--root user-space/<别人的 ns> open-append` 真写了 2 B 进 `src/userspace/`**）⇒ `_inside_zone`（词法 × 真实两形态交叉各判一次，**收紧而非放宽**）+ 门第 8 条**反向自证** + 新增 `--mutate 5`（必红，首个 FAIL = 第 8 条，还原 sha256 一致）；门 **18/19 → 19/19**。另：`AC-RUNTIME-001` 扫描面收紧到实体目录（62 文件）；`plugin-file-map.md` 48825/49152 B ⇒ 批次叙事 5976 B 拆到 `plugin-file-map-batches.md`；`plugin-assets` **14/14**（RELOCATED 49→74，非薄入口仍 64 ≤ 基线，**未放宽**）。原文见 `EV-174-batch4-raw.json` | AC-PLUGIN-004 / AC-RUNTIME-010 / `T-323` | P2 |
| EV-173 | **迁移阶段 4.2 续批 + `qa` 检查搬家 + 阶段 5 第一小片**（`T-322`）：① 剩下的 10 个 `host/*-gate.mjs` 搬进各自 `tests/`（至此 **20/20**；旧位置 5 行薄转发，`verify.sh` 分支一行未改，逐项 rc/passed/total/断言名与 ok 对拍一致）② **21 个** `src/quotagent/qa/checks_*.py` 搬进各自 `tests/`（`importlib` 薄转发保导入面；31 条相关 AC 全部 `names_same/ok_same`）③ 内核实体 `src/quotagent/kernel/**`（9 个 `.py`）搬进 `src/system/kernel/code/`，旧路径留**薄重导**（`import quotagent.kernel.*` 与相对导入不变，逐条 sha256 守恒）④ 修 3 处「检查器读旧门路径 ⇒ 读到薄转发」的真缺陷（`AC-QUOTE-001`/`AC-UIFB-001` 由红转绿，`p0-no-node` 恢复绿）⑤ `plugin-assets` 14/14（RELOCATED 49 项、基线仍 64 未放宽）。原文见 `EV-173-batch3-raw.json` | AC-PLUGIN-004 / AC-DESIGN-001 / `T-322` | P2 |
| EV-172 | **每个插件自带测试与需求 → 实体标准布局**（`T-321`）：① 收紧「目录存在 ≠ 插件存在」（`scan` 只认有 `plugin.json` 的目录、`depsClosure` 只认**合法清单**）+ 反向验证（假插件 ⇒ `missing_targets` 仍 `["system/webui"]`、`status` ⇒ `unknown-plugin`；叠加真插件目录 ⇒ 正常识别）+ `plugin-lifecycle` 新增 A3c/A15–A18 与变异 5/6 ② 58 份需求文档 `git mv` 进 `src/<层>/<插件>/requirements/README.md`（逐字节守恒）+ 54 个插件补最小 `plugin.json`（如实 `degraded: artifact-missing`）+ §4.2 改归位台账（A6c 三条双向断言，门 18/18）③ 10 个围栅门搬进各自 `tests/`（旧位置薄转发，rc/输出形状逐项对拍一致；`plugin-assets` 14/14，基线未放宽） | AC-DESIGN-001 / AC-PLUGIN-004 / T-321 | P2 |
| EV-171 | **「克隆就能一键跑」落成可重现的干净副本验收**（`T-320`）：① 新门 `run-clone`（`tools/check-run-clone.py`，K1–K12）—— `git archive HEAD` 解到**仓库外**临时目录（只含已提交内容：无 `.venv`/`host/node_modules`/`tmp`；路径集合 == `git ls-tree -r HEAD` 逐条对账），在其中真跑 `doctor`（7 项逐项 next_action + 退出码 0）/`up`（一条命令：外部实测 health 200 + `/quotagent/` 200 + pid 是活进程 + 副本内自建 `.venv`）/`status`/二次 `up` 幂等（两次 status 逐字节一致）/`down` 真释放端口；② **实测真缺陷并修复**：索引里 `tools/*.sh` 是 `100644`（`core.filemode=false` ⇒ `chmod +x` 不入库）⇒ 干净克隆 `./run up` 报 `host-deps-install-failed`、`doctor` 的 gates 项 FAIL；`git add --chmod=+x` 7 个脚本，并把「入口可执行位」冻结成门内 K2 断言（并在**未修的 HEAD** 上先跑过一次：门精确红在 K2/K3/K4）③ `./run` 的依赖准备失败改为**可诊断**（原样输出落 `tmp/run/deps-install.log`；失败体带 `log` + `log_tail`；实测三种失败都给出真原因：`npm error code ENOTCACHED` / `/usr/bin/npm: not found`）④ `doctor` 的 cordis 项区分「缺失但能在 up 内自动准备」（`degraded` + `will-install-on-up`，不阻塞）与「无 npm」（`FAIL`）⑤ 两条反向对照真跑（缺依赖如实报；空缓存 + 断网垫片 / npm 不可用 ⇒ 如实失败）⑥ README 快速开始（克隆 → 一条命令 → URL → 凭据降级与配置 → 下一步看哪里；4052/4096 B）⑦ `plugin-assets` 基线 63→64 + 本门登记 | AC-RUNTIME-010 / `T-320` | P2 |
| EV-170 | **迁移阶段 4.1 先行 8 项：散落的检查/测试资产收进各自插件的 `tests/`，`tools/**` 瘦成薄入口**（`T-319`）：① 清点 143 项（`tools/**` 76 / `host/*-gate.mjs` 20 / `qa/checks_*.py` 47）逐项分类进 `plugin-file-map.md` §分类（薄入口 6 / 已搬 8 / 待搬 129，与磁盘双向）② 搬 8 项 → `src/<层>/<插件>/tests/`，旧位置只剩薄转发（`verify.sh` 门名与分支一行未改，逐项对拍一致）③ 新门 `plugin-assets` **14/14**（PA1–PA7 + 4 处单点变异全红 + 产品树字节不变）④ 4 个插件骨架真装卸 ⑤ `tools/**` 非薄入口 69→63 | AC-DESIGN-001 / `T-319` | P2 |
| — | 更早的行（EV-156 / EV-157 / EV-160 / EV-162 / **EV-168 / EV-169**）分别整行逐字搬入 [`INDEX-P2-archive.md`](INDEX-P2-archive.md) 与 [`INDEX-P2-archive-b.md`](INDEX-P2-archive-b.md)（每份分册各自受 8 KB 预算约束；本页加 EV-174 行时把最早两行搬去 `-b`） | — | P2 |

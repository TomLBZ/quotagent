# 证据索引归档（P2 product：从 `INDEX-P2.md` 拆出的更早行，**整行逐字未改**）

> 为什么拆：`docs/work/evidence/*.md` 的预算是 8 KB（`12-documentation-standard.md` §1），`INDEX-P2.md` 到顶后新证据没有位置 —— 与 `INDEX.md` 分册到 `INDEX-P1.md`/`INDEX-P2.md` 同一做法。

| 证据 | 内容 | 关联 | 阶段 |
|---|---|---|---|
| EV-157 | **「来不及回 RFQ」**（`rfq-deadline`，正面回答 P-10「截止时间与催报没有入口」+ P-04「被迫等回到电脑前再算、错过截止」）：围栏门 **23/23**（**期限不随窗口变化**：两个墙钟入口各给两个不同值 ⇒ 输出逐字节一致，`remaining_seconds` 与手算 `due_ts − as_of` 逐条相等；**没凭据不得假装能发**：`available=false` ⇒ `blocked_by` 写「无法代发」，整个输出里「已通知/已提醒/已发送/已发出/已催」**0 命中**；**名册白名单**：非业主视角对 `invited`/`quotes` 读都不读且与 `sourcing.coverage()` 同一口径；空输入三种降级各有名 reason 且条目为空；确定性 / 有界 + `omitted` / 私域哨兵带与不带逐字节一致 / 零写面 / 四道页面子导航入口 / 页面模板 0 内联脚本；**4 处单点变异全红** + 防假变异自检 + 还原字节一致）+ 真路由门 **11/11**（真进程真回读：两视角页面与 JSON 200、剩余时长与手算一致（302400 / -1800，过期那条 severity=overdue）、空投影降级且条目 0、登记承诺 POST **202** + 待办件**恰 0600** + 原话逐字 + 宿主**账本零新增**、真跑 `rfq-promise.py` 落 `rfq/promised`（body 恰 6 键、不含原话正文）且 `/api/status` 计数 **5 → 6**、**承诺真的改变页面口径**（`due_basis` 换 `rfq/promised`、剩余 352800）、幂等 `already-promised`、拒绝路径 `rfq-not-found` / `pending-tampered`、私域哨兵 0 命中） | AC-RFQ-006 / T-285 | P2 |
| EV-160 | **「供应商看不到自己的 RFQ 包」的根因修复**（`FR-RFQ-009`，契约 `docs/design/26-rfq-delivery-visibility.md`）：投递信封 + **收件人作用域**（身份只来自本视角自己的账本）+ **字段级白名单**（✓ 包 id/rev/报价与澄清截止/行项目与数量/币种/发放时刻/发放对象只出自己，恰 9 键；✗ 承包商私域与他家供应商数据**读都不读**）。围栏门 **26/26**（**被邀供应商看得到包**（与夹具事实逐字相等）/ **未被邀的供应商看不到**（`packages` 空 + `reason=no-deliveries-visible`）/ **同一次调用里三方对比** / 他家代号与他家包 0 命中 / 承包商私域键与哨兵值 0 命中（非空转对照）/ **带哨兵与不带哨兵输出逐字节一致** / rev 与截止逐字取事实且源码 0 墙钟入口 / 确定性 / 同包多版本取最新 / 有界 + `omitted` / **7 个有名降级 reason** / 派生行 `seq:null` 且账本行不增不减 / 承包商侧不减少 / 零写面；**4 处单点变异全红**（含「把包发给所有供应商」的越权变异）+ 防假变异 + 还原字节一致）+ 真路由门 **10/10**（真起两个进程、两种身份：被邀的事件里出现 `rfq/published` 且字段与手写夹具逐字相等 + 首页 `data-rfq-*` 抓手齐 / 未被邀的 `rfq/*` 0 条 / **追加只发别家的信封后输出逐字节不变**、追加同时发两家的信封后必须变化 / 坏信封不崩服务 / 哨兵 0 命中 / 承包商侧不减少 / 只读（账本与信封前后逐字节不变）+ 同 URL 两次一致 / 0 内联脚本 / 未提权 `/admin/` 401 固定体）。重启后真回读原始行：`tmp/rfq-visibility-run.out`（供应商首页 「最新 RFQ 包：`pkg-g1@rev1` 报价截止 `2026-09-25T00:00:00Z`」+ `/supplier/api/events` 出现 `rfq/published` 派生行） | AC-RFQ-007 / T-287 | P2 |

# 证据索引（P2 product 分册 C：EV-181 起）

> 为什么要开分册：`docs/work/evidence/INDEX-P2.md` 已到 8046/8192 B（8 KB 硬预算），无法再追加行；
> 分册与 `INDEX.md` → `INDEX-P1.md` → `INDEX-P2.md` → `INDEX-P2-archive*.md` 同形，每份各自受 8 KB 预算约束。
> 每份证据的原始命令输出在 `EV-<编号>-*.txt`。

| 证据 | 内容 | 关联 | 阶段 |
|---|---|---|---|
| EV-181 | **干净副本红 AC 的根因 + 7 个 `entry` 决定 + 硬编码路径门**（`T-331`）：① `AC-AGENTRT-002/006/007` 在 `git archive HEAD` 副本里红的**根因** = 围栏门顶层 `import` 宿主内核 `cordis`（`host/node_modules/` **gitignored** ⇒ 副本里不存在），三条 AC 原先裸 `node <实体>` ⇒ `ERR_MODULE_NOT_FOUND`；「单独跑也全绿」是因为全量按 ID 升序跑、后面的 `AC-INTEG-004` 走 `tools/cordis.sh run`（幂等 `install_deps`）把依赖装上了（实测：跑前 `host/node_modules` 不存在、跑完存在）。修法 = 三条 AC 改走既有的 `tools/cordis.sh run` 约定，**判据一格未改**；正向（同副本 3/3 PASS + 全量 **85/85**）/反向（回退成裸 `node` ⇒ 3/3 必红） ② **7 个多实体插件定 `entry` 并接上**（D-078..D-084）：单实体 = 薄重导出（`webui`/`mail`）、多实体 = **机制组合**（`admin` 2 个 / `agent-runtime` 3 个 / `canary` 2 个）、宿主侧 0 个 ESM 服务实体 = **Python 承载入口**（`eval` 新建 `code/__init__.py`、`kernel` 用它自己的包初始化）⇒ `plugin.sh list` **63/63 valid、degraded 0**（此前 7 条 `artifact-missing`）；连带把 `plugin-lifecycle` 的 A13/A14 从「真根上 webui 未迁移」改指**对照根负控**（真 id + 真清单字节 + 入口缺失）+ 真根正控，**断言 66/66 → 68/68**（只增不减），同一运行的变异 1..6 仍全红 ③ 硬编码仓库根扫描（`src/**`/`host/**`/`tools/**`/`*.sh`）：命中 4 处（`mutate-ui-views.py:16`、`ui-feedback-tick.sh:11`、`ui-feedback-monitor.sh:10`、`manual-check.py:15`）全改成由 `__file__`/`$0` 推导；新增门断言 **PA9**（以 `quotagent` 为末段目录的绝对路径字面量 0 处 + 探针自证非空转）+ 门内变异 **F8**（写死 checkout 路径 ⇒ PA9 必红）；`plugin-assets` **16/16 → 18/18** | `T-331`, `AC-PLUGIN-004`, `AC-AGENTRT-002/006/007` | P2 |
| EV-190 | **业务断点与身份缺口（P42 批前置）**：① 报价行带 `qty`（「从界面提授标意向」真落账）② 澄清入口合并 ③ **业务路由身份门槛**（`auth=identity-session`，与 `code/identity.mjs#gateBusinessRoute` 同一判据）④ 按 id 找对象（深链 / 对象页）。**验证不在共享工作树上做**：同树有 sibling 写者，改用 `git archive HEAD` + 本批补丁的副本（`tmp/verify42/`），门的冲击清单取自 `tmp/gatecheck/`。缺陷登记 DEF-001/009/014/015 | `29` §2.2/§3/§4 | P2 |
| EV-191 | **身份门槛导致的红断言：门夹具先「登录」再取业务路由**（12 个门；**断言只增不减、删除 0 条**）：`EV-190` 登记的 9 个门 **63 条红 → 全绿**（没把 `200` 放宽成 `200|401`）；同一根因还遮住 4 个门的后半（HEAD 版单跑红 6/7/8/8，`change-detail` 后半直接崩）+ `EV-190` 漏登记的 3 个门，一并修好。夹具口径 = 先 `POST /identity/login` 再取业务路由（「登录后按侧放行」）；仅当断言本质上在断言「未登录也能看业务页」才按规则 12 删除（本批**删 0 条**） | `29` §2.2、`EV-190` §5 | P2 |
| EV-192 | **P36 全量回归与 `tmp/` 清理**：套件 `s1..s4` 16/16 断言、`rc=0`（两轮）；**逐门 58/59**（红 `clean-copy`/`evolve-module`/`p0-no-node`，`g1` 含 `qa all` 81/83 —— **无一是清理引入**）；双岗位走查 `verify-identity` **49/49**（沙盘 seed 红 = `writer-gate-timeout`，P21 引入）；`tmp/` **4.4 G → 183 M**（删派生整树 / 变异树 / 旧夹具树 / 0 消费者产物；留活文档引用到的可复跑脚本与当前批次证据）。只读回归 + 清理，不改产品行为、不新建门 | `EV-191`、`29` §2/§21、`12` §1 | P2 |
| EV-193 | **29（含归档）承诺↔实现逐条对账**（P45 批，未提交）：把 29 主文件 + 归档抽成 **157 条可检验承诺**（P46 追加 `C158` ⇒ **158 条**）（按 § 分类：§0..§23），逐条在实现里解析**承接点**（`file:line`）= 承接 **152** / 缺口 **0** / 漂移 **5**；5 条漂移逐条给证据并**当场改完**：① 对比度「零不达标」与原始读数冲突（`tmp/p7-a11y-out.txt` 的 PO 对象页 2 处 1.51）⇒ 29 §12 与归档 §12.5 改「1 处例外已登记」 ② §15「48 条断言」⇒ 实测 49（`tmp/verify-identity/verify-result.json`）⇒ 改 49 ③ 归档 §12 复跑指针 `tmp/p7-report.md` 不存在 ⇒ 改指在盘读数 ④ 归档 §20 反证读数指针 `tmp/p21-shots/concurrent-broken-chain.json` 不存在 ⇒ 改指 `tmp/p21-scale-run/webui/jobs.json`（`ledger-frozen` / `seq 309`）⑤ rfq 插件**动作载荷**两处 `next_action` 教用户回终端（`python3 …`，违反 §21.4）⇒ 换成界内下一步。可重跑（任意 cwd）：`python3 src/system/repo-gate/tools/claim-impl-audit.py`（承诺与 probe 的真源 = `docs/work/evidence/EV-193-claims.json`；**P46 复跑 158 条 = 承接 153 / 缺口 0 / 漂移 5、rc=0**，并补登记 §24 两支审计尺子 = `C158`） | 29 §1..§23 / `AGENTS.md` 规则 11/12 | P2 |

| EV-194 | **P46：三处遗留收尾 + 两支审计尺子进仓**：① 待办卡/门对象页「请求人」= `body.requested_by ?? row.actor`（**新口径行显示真身份**、旧行照账本 —— 真起服务 + 真登录 + 真读接口，三个夹具：新行 `human:wanglei` / 旧口径行 `human:p21-supplier` / 旧服务行 `agent:approval`）② **计数器根因**：`ApprovalService.__init__` 的 `_counter = 0` 挪到 `replay()` 之前（同账本正/反两边：修后 `ap-0275`，旧顺序 `ap-0001` 重号复现）③ **两支尺子进仓**（`src/system/repo-gate/tools/body-keys-audit.py` 偏差 3 全是「多写者形状不同」；`claim-impl-audit.py` 158 条 = 承接 153 / 缺口 0 / 漂移 5、rc=0；承诺真源搬进 `EV-193-claims.json`，**何时跑/怎么读/偏差处置 = `29` §24**）④ `handover`/`progress-checklist` 腾字节补行 + 证据索引补齐 EV-190/191/192 | `29` §24、`AGENTS.md` 规则 11/12 | P2 |

> 登记滞后披露（P46 补）：`EV-190`/`EV-191`/`EV-192` 三份证据此前**从未进索引**（证据文件在盘、索引没行），本次补齐；`EV-193` 行同步为 P46 复跑读数。

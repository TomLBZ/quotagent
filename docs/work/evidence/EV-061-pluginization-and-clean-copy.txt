EV-061 插件化（清单门/自动发现/clean-copy 门）+ timeline 中间件（T-225/T-226）
生成时间：2026-09-21T08:25:47Z（UTC）

约束（用户 2026-09-21）："项目的每个功能都应被插件提供"、"可以用 subagents 制作 domain based 插件或中间件"、
"每个功能模块可分别独立演进"。以下为原始命令输出。

[1] tools/verify.sh plugins（插件清单门：目录↔清单、模块↔profile 装配、Python 服务归属）
[FAIL] P1 目录→清单：每个进树模块在清单里有一行（新增功能必须登记归属）
        模块 ['compare', 'kernel-bridge', 'norm', 'sourcing', 'timeline', 'webui']；未登记=['sourcing']
[ok]   P1 清单→目录：清单里的模块行都有对应文件（不得引用不存在的插件）
        清单里多出=[]
[FAIL] P2 模块→装配：每个模块至少被一个 profile 引用，或在清单里显式标「未接线」
        profile 引用=['approval', 'compare', 'config', 'cost', 'frozen', 'guard', 'kernel-bridge', 'norm', 'pricing', 'queue', 'timeline', 'transport', 'webui']；未接线=['sourcing']（已显式标注=[]）
[ok]   P3 Python 功能归属：每个 `services/*.py` 在清单里出现（功能有归属）
        服务 23 个；未归属=[]
RESULT: FAIL（插件清单门 2/4）
（exit=1）

[2] tools/verify.sh modules（进树模块 fixture A1..A6；含 subagent 交付的 timeline 中间件）
0}
[ok] webui · A3 — A3 config 负控：未知键被拒（本模块无 const 键，const 负控在模块集级断言里）
        unknown=refused constKey=（无） flip=n/a
[ok] webui · A4 — A4 事件声明：源码里 emit 的事件名必须都在事件表里（真源在 Python 侧，静态扫描）
        emit=[] 未声明=[]；事件表 89 条
[ok] webui · A5 — A5 确定性：同输入两次输出字节一致（无墙钟/无自增序号）
        len=35 equal=true
[ok] webui · A6 — A6 无跨模块 import：不得出现指向别的模块目录的相对 import（只允许 ../lib/ 与包名）
        imports=["node:http","../lib/std-schema.mjs","../lib/ledger-view.mjs"] 越界=[]
[ok] __set__ · A3 — A3 模块集级：至少有一个模块暴露 const 键并拒绝翻转（否则 const 纪律无人覆盖）
        const 键：compare.deterministic, kernel-bridge.credit_window, norm.enforce_tolerance, sourcing.deterministic, timeline.deterministic
------------------------------------------------------------------------
模块：7 个（compare, kernel-bridge, norm, sourcing, timeline, webui, __set__）；事件表 89 条（真源：Python 侧）
fixture 断言：73/73 通过；报告落 /workspace/projects/quotagent/tmp/modules-report.json
（exit=0）

[3] tools/verify.sh clean-copy（把**已提交内容**解到干净副本后跑关键门；抓"本地能跑、克隆就废"）
干净副本：/workspace/projects/quotagent/tmp/clean-copy（258 个文件，仅已提交内容）
[FAIL] 自足性：8/9 个必需文件在副本里；缺 ['host/modules/index.mjs']
[ok] docs: exit=0 | RESULT: PASS
[ok] cordis 冒烟: exit=0 | }
[ok] 事件门: exit=0 | [ok]   文档与事件表完全一致（名称与模式）
[ok] 进树模块 fixture: exit=0 | fixture 断言：49/49 通过；报告落 /workspace/projects/quotagent/tmp/clean-copy/tmp/modules-report.json
[ok] WebUI 门: exit=0 | RESULT: PASS（webui 门 11/11）
[ok] 不变量门: exit=0 | 宿主强制不变量：22/22 通过
[ok] 演化门: exit=0 | }
[ok] AC 注册表: exit=0 | [ok]   P0 阶段的 AC 全部已注册（无「有编号无断言」）
[ok] 无 Node 下的 P0: exit=0 | P0 阶段 35 条 AC 在无 Node 环境下全绿（P0 不因引入宿主而失去可复跑性）
RESULT: FAIL（缺文件 1；失败门 []）
（exit=1）

结论：
- 插件清单门把"每个功能有归属"变成机检：新增 `host/modules/*.mjs` 必须登记，且必须被某个 profile 装配
  （实测抓到 `kernel-bridge` 从未被任何 profile 引用 → 已补两侧装配声明）；
- 模块自动发现（`host/modules/index.mjs`）+ fixture 采样点下放到模块（`fixture.sample`）：新增功能不必改中心清单
  或检查器；
- clean-copy 门抓到的历史缺陷：`.gitignore` 的 `lib/` 通配吞掉 `host/lib/*.mjs` 整层（本地正常、克隆缺库）→ 已修
  （根目录限定 `/lib/` 并把 8 个库文件入库）；
- clean-copy 里 `modules`/`webui` 门曾因干净副本无 `node_modules` 而 exit=2 → 两道门改为经 `tools/cordis.sh run`
  / `cordis.sh install` 自愈依赖（门必须能自己把环境补齐，不能把环境问题当代码问题）。

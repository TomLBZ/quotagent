# V-003 采购负责人评审表（AI 排序建议 + 人工签署）

<!-- 目的：是否接受『AI 排序建议 + 人工签署』。判定：≥ 1 名愿意在真实包上试用 -->
评审人：____（角色：采购负责人）  机构：____  日期：____

## 先给对方看的三样东西
1. S1 比价的排序建议（`tools/run.sh -m quotagent.qa suite s1` → `facts.ranking`）
2. 每条报价的 TCO 分量表（价格/交期/付款/质保/偏差，逐项有引用）
3. 两条硬规则说明：**排序只是建议**；**最终数字与授标必须人签**（规则 3 / INV-005）

## 记录（原话）
- R1 这套排序结果，您觉得**可信**吗？哪里最不信？
- R2 您愿意用到哪一步？（只看建议 / 采纳排序 / 在真实包上试用 / 不愿意）
- R3 TCO 的权重（价格 0.6、交期 0.15…）如果由人设定，您希望谁定？
- R4 如果每条建议都能点开看到出处（账本条目 + 清单条目），会改变您的判断吗？
- R5 最担心什么？（写原话）

## 判定
愿意在真实包上试用的评审人数 ≥ 1 → `pass`；只愿意"看建议" → `partial`；全部拒绝 → `fail`。
证据：`docs/work/evidence/EV-<编号>-V-003-<姓名或代号>-<YYYYMMDD>.txt`

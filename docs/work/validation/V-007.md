# V-007 · 协作确是跨法人（非同一集团内部）

<!-- budget: 16 KB。结论状态见 register.json；结论由人签，agent 只备料与登记 -->

## 假设与判定

- **假设**（`functional-requirements.md` §1）：协作确是跨法人（非同一集团内部）
- **验证方式**：确认参与方法律关系与结算主体
- **判定标准**：跨法人则 ADR-0001/0003 前提成立
- **影响**：影响隔离模型与账本同步
- **判定态**：`pass` / `fail` / `partial`（部分成立时写 `partial` 并在结论里注明范围）

## 需要谁

- 商务/法务 1 人（15 分钟）

## 准备的材料（已备好，可直接用）

- `templates/v007-legal-relation.md`

## 怎么做（步骤）

1. 列出实际参与方：承包商、供应商（各 1–3 家），写清**签约主体**与**开票主体**是否同一。
2. 确认是否存在同一集团内的关联方（若有，注明哪些环节在集团内）。
3. 存证据 `docs/work/evidence/EV-<编号>-V-007-<来源>-<YYYYMMDD>.txt`（可只写结论 + 依据，不必附合同）。

## 产出与例子

- 结论写进 `docs/work/validation/register.json` 的 `checks[V-007]`：`status` + `results` + `conclusion` + `decided_by`（必须 `human:*`）+ `decided_at` + `evidence[]`。
- 证据文件命名 `docs/work/evidence/EV-<编号>-V-007-<来源>-<YYYYMMDD>.txt`（原始表格/问答摘录/截图说明都可以，但必须是真实产出）。

- **例子**（**合成示例**。）

```json
{
  "id": "V-007",
  "status": "pass",
  "results": {
    "parties": 3,
    "same_group_adversarial": false,
    "metric": "是否跨法人",
    "value": "跨法人（3 个独立主体）",
    "criterion_met": true
  },
  "conclusion": "（一句话结论，写清是否达到判定标准）",
  "decided_by": "human:<姓名>",
  "decided_at": "2026-09-30",
  "evidence": [
    "docs/work/evidence/EV-<编号>-V-007-<来源>-<YYYYMMDD>.txt"
  ]
}
```

## 放回哪里（登记步骤）

1. 结果位置：`register.json` → checks[V-007]；证据 `EV-<编号>-V-007-*`
2. 填完跑 `tools/verify.sh v`：全绿说明登记合法（缺证据/缺签字/留空都会红）。
3. 一轮一批：`git add -A && git commit -m "docs(validation): V-00X 结论（<来源>）" && git push`。
4. 12 条都填完后，`docs/work/progress-checklist.md` 的 T-117 才能标 `done`，G0 门由人签（`roadmap.md` §2 门 G0）。

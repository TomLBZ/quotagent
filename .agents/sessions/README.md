# 轮次记录

<!-- budget: 4 KB. 一行一轮次；这是索引，不是日志。 -->

格式：`YYYY-MM-DD | 轮次 | 做了什么 | 验证结果 | 下一步`。
只记事实与下一步；理由进 ADR，状态进 `../../docs/work/progress-checklist.md`。

| 日期 | 轮次 | 做了什么 | 验证 | 下一步 |
|---|---|---|---|---|
| 2026-09-11 | R1 | 分析层（4 篇）+ 设计层（13 篇 + ADR 0001..0006）+ FR/AC/roadmap/checklist/handover + 文档门工具 | `tools/verify.sh docs` PASS（EV-001）；远端已推送并回读（EV-003） | T-101（P0 S0.1）+ T-117 的 V-002 盲测 |
| 2026-09-12 | R2 | T-101（自包含运行时 `tools/runtime.sh|run.sh|bootstrap.sh` + CLI 骨架 + ADR-0007）、T-116（文档门干净环境复跑）、T-102（账本：追加/哈希链/投影重建/去重/停发；模型调用落账与输入重建） | `tools/verify.sh docs` PASS（EV-004）；`ac AC-AUDIT-001/002`、`ac AC-RUNTIME-001/002` 全绿（EV-005..EV-008） | T-103（P0 S0.3：事件五模式 + disposer） |
| 2026-09-12 | R3 | T-103（事件五模式 + 声明表 + 短路记录）、T-104（插件 fiber 状态机 + 依赖失活/重载不迁移草稿 + effect 回收）、T-105（QEP 信封/验签/幂等 + 文件原子投递；ADR-0008） | `tools/verify.sh docs` PASS（EV-004）；7 条新 AC + 7 条回归全绿（EV-009..EV-015） | T-106（P0 S0.6：归一化与拒绝语义） |
| 2026-09-12 | R4 | T-106（归一化五段 waterfall + 拒绝语义 + 口径数据层）、T-107（包版本化：只读视图/字段级 amend/发布校验）、T-108（读包抽取 + [假设] 门 + 缺项 + 疑问人工确认；ADR-0009） | `tools/verify.sh docs` PASS（EV-004）；7 条新 AC + 14 条回归全绿（EV-016..EV-022） | T-109（P0 S0.9：成本构成私域） |
| 2026-09-12 | R5 | T-109（成本构成七要素 + 私域哈希引用 + realm 三处过滤点）、T-110（定价五段 waterfall + 越界请求批准 + 人确认落定 + 承诺唯一出口）、T-111（四类偏差捕捉 + 三维量化 + 未标 impact 不进 TCO；ADR-0010） | `tools/verify.sh docs` PASS（EV-004）；6 条新 AC + 21 条回归全绿（EV-023..EV-028） | T-112（P0 S0.11：比价 TCO + 排序 + 引用链） |
| 2026-09-12 | R6 | T-112（五项金额化 TCO + 极差归一评分 + 引用链硬约束 + 版本失配挂起）、T-113（护栏六规则只标注 + S4 反例集只增不减）、T-114（场景集 S1..S4 + `qa suite s1..s4` 一条命令 + digest 确定性）、T-115（九项指标 + 基线报告入库；ADR-0011） | `tools/verify.sh docs` PASS（EV-004）；7 条新 AC + 27 条回归全绿（EV-029..EV-035）；`suite s1..s4` PASS | T-117（现场验证，需人工） |
| 2026-09-12 | R7 | T-117 备料：12 份 V 执行包 + 13 份模板 + 1 份填好示例（`docs/work/validation/`）、登记表 `register.json` + 校验器 `tools/verify.sh v`、打包工具 `tools/v-kit.sh`；agent 自检 V-002（表单可被读入）与 V-011（投递+验签） | `tools/verify.sh docs` PASS（EV-004）；`tools/verify.sh v` PASS（12 条 `open`）；自检 EV-036/EV-037；校验器负控（伪造结论/代签/缺证据均变红） | **转人工**：按 `docs/work/validation/README.md` 的接手指令执行 V 项并签字 |
| 2026-09-12 | R8 | P1 开工：宿主层直接依赖 cordis 4.0.0-rc.10（`host/` + 冒烟 18 断言 + EV-038 + ADR-0012）；三份独立 agent 评审（桥接/切片/模块演进）入库 + 决策日志；ADR-0013 桥接协议、ADR-0014 P1 前提与 MVP 判据；register 加 `planning_assumptions`（标 not_a_conclusion）并把校验器收紧 | `tools/verify.sh docs` PASS（73 md、0 未解析）；`tools/verify.sh cordis` PASS；`tools/verify.sh v` PASS（含两条新负控）；P0 34 条 AC 仍全绿 | **批次 B1**（文档覆盖缺口）→ **B2**（T-201 双侧进程分离 + profiles，AC-PLUGIN-003） |
| 2026-09-21 | R9 | B1 文档覆盖缺口：实测 31 条"无 T 行"的 FR 里有 25 条是**同族区间记法**（前缀+起止序号）造成的误判，真缺口 6 条已归属（FR-QEP-005/006 归 T-202、FR-COST-003 归 T-109、FR-GUARD-004 归 T-211、FR-DEV-002 归 T-111、FR-CLARIFY-004 归新 T-310）；roadmap §3 十四条 P1 步补齐"完成标志"；AC 契约表补 g1/cordis/v；新增 `docs/work/reviews/*.md` 预算行 | `tools/verify.sh docs` PASS（72 文件、0 未解析） | B2（T-201，AC-PLUGIN-003） |
| 2026-09-21 | R10 | B2/T-201 完成：host profiles=组成数据（三 profile，一 profile 一真进程/一 realm/一账本）；配置更新改用 cordis 原生 `fiber.update`+`internal/update` 瀑布（守卫不调 `next()` 即否决 → 配置未变、插件不重启；生效即重启是上游语义）；三档可改性 + 键白名单（H10）；`host/cli.mjs` boot/status/config-update（stdout 一行 JSON）；AC-PLUGIN-003（11 断言，宿主=被测进程）；ADR-0015 + `host/CONFIG.md` | `tools/verify.sh docs|cordis|v` PASS；**35 条 AC 与 s1..s4 全绿**（EV-039） | **B3/T-202**（QEP 跨进程投递 + receipt + 重发 + seq 空洞） |
| 2026-09-21 | R11 | B3/T-216 桥接最小闭环：`src/quotagent/bridge.py`（内核首帧自述能力清单；版本交集为空→退出码 2 且账本 0 字节；方法面 read/compute；commit 面声明但拒绝 + 留痕；身份由内核注入，宿主自称 human 即拒；帧上限 8 MiB；固定错误码 + next_action）+ 宿主侧 `host/lib/bridge.mjs`/`cli.mjs bridge`；五个 `kernel/bridge-*` 事件登记进 `05-events.md` §2 与 `kernel/events.py`；`docs/design/13-cordis-bridge.md`；FR-INTEG-004 + AC-INTEG-004/005；`tools/verify.sh bridge` | `verify.sh docs|cordis|bridge|v` PASS；**37 条 AC 与 s1..s4 全绿**（EV-040） | **B4/T-217**（桥的故障语义）→ B5/T-202（QEP relay/重发/空洞） |
| 2026-09-21 | R12 | B4/T-217 桥的故障语义：宿主监督者（重启预算 3/30s→只读降级、锚点比对、在途记 unknown、无孤儿关闭）+ 内核侧 `--restarts/--anchor/--anchor-seq/--read-only/--credit-window` 与 `kernel/bridge-restarted|fault|backpressure`（背压按回合留痕，含丢弃计数与时间窗；durable 靠 ledger.read 补齐）；7 个故障场景入口 `host/cli.mjs supervise`；AC-INTEG-006（13 断言）；`verify.sh p0-no-node`（P0 34 条在无 Node 下全绿）+ AC-RUNTIME-001 静态断言（P0 不引用 node/cordis）；修掉一处**假绿灯**（阶段过滤写错导致"没跑到"当成"全绿"） | `verify.sh docs|cordis|bridge|p0-no-node|v` PASS；**38 条 AC 与 s1..s4 全绿**（EV-041） | **B5/T-202**（QEP relay/receipt/重发/seq 空洞） |
| 2026-09-21 | R13 | B5a/T-202（内核部分）：QEP 顺序化 —— per-sender `expected seq`，空洞即**挂起**（不落事实）并发 `relay/resend-request`；补齐后按序应用并落 `kernel/qep-gap-filled`；回执 `relay/receipt`（传输确认，控制类报文不互相回执，避免无界乒乓）；`negotiate()` 取版本交集最大值、特性差异落 `kernel/qep-degraded`、批准链/签名/版本绑定缺失即拒；`resend_unacked()` 阈值重发（内容不变）。踩坑：第一版先排空挂起再推进 expected → 挂起项永不落地（已修）；`_reject(code=)` 缺参数（已修） | `qa ac AC-QEP-003`（14 断言）+ `AC-QEP-004`（6 断言）；**40 条 AC 与 s1..s4 全绿**（EV-042） | **B5b/T-218**（relay 绑定 + AC-INTEG-002） |
| 2026-09-21 | R14 | B5b/T-218 relay 绑定：`services/relay.py`（opaque 字节转发，自有 realm 账本，只记整包 sha256、`parsed=false`；目标不可达→`relay/queued`→`pump()`→`relay/retry`/`relay/delivered`；spool 被改→`relay/tamper-detected` 拒投）；AC-INTEG-002（11 断言）覆盖 relay 照常转发篡改包 + 接收方验签拒收、字节透明、发送侧不可达不落半条记录且重发幂等、账本隔离；`04` 补 `ctx.relay` 条目 | `qa ac AC-INTEG-002` 全绿；**42 条 AC 与 s1..s4 全绿**（EV-043） | **B6/T-203**（三方协调 + 字段权威方 + 冲突上报） |
| 2026-09-21 | R15 | B6/T-203 账本同步：`services/sync.py`（三方协调逐字段三值判定；冲突按 §4.3 权威方矩阵裁决；承诺字段与矩阵未覆盖字段 → `pending_human` + 条目挂起，**不自动合并**；人工裁决需双方各一次 `human:*` 批准，单侧/agent 代签不算；非权威字段改动转 `intent/suggestion` 且不进 base）；事件 sync/merged|conflict|suspended|suggestion-raised 登记；`04` 补 `ctx.sync`。踩坑：AC 里"base 被后一次 reconcile 覆盖"导致断言取键失败（改为不依赖历史）；"单侧改"与"冲突"的边界要先想清再写断言 | `qa ac AC-SYNC-001`（15 断言）；**42 条 AC 与 s1..s4 全绿**（EV-044） | **B7/T-204**（澄清工单 + 广播完整性 + 版本重开） |
| 2026-09-21 | R16 | B7/T-204 澄清工单：`services/clarify.py`（建单必带 rfq_rev + 条目引用，否则拒绝留痕；作答草稿过 `clarification/answer-drafted` waterfall 拦私域，strict 短路/非 strict 剥字段；回答者必须 human；广播按**在册投标人集合**校验完整性 → 缺任一落 bail 事件且 close 拒绝；包升版 `on_package_rev` 自动重开 + 旧答案 stale/applies_to_rev + 清空广播）；事件 clarification/rejected|reopened 登记。顺带发现漂移：**AC-CLARIFY-001 文档标 P0 却从无断言** → 已实现，并新增 `tools/check-ac-registry.py`（负控改名后变红）。踩坑：bail 模式事件不能用 emit 派发（内核直接抛 EventModeError，改为按 @mode 分发） | `qa ac AC-CLARIFY-001/002/003` 全绿（8+3+4 断言）；**45 条 AC 与 s1..s4 全绿**（EV-045） | **B8/T-205**（包版本变更与报价过期标记） |
| 2026-09-21 | R17 | B8/T-205（+B9 的前置）：`services/quotes.py`（包升版 → 旧版本报价标 stale/superseded_by_rev 并落 `quote/superseded`；重报请求带旧/新版本号；链式升版与幂等；标记不清除报价）；`compare.rank(..., book=)` 以 `quote_superseded` 显式排除；新增 FR-RFQ-006/AC-RFQ-004。**修真实缺陷**：bail 模式事件（`rfq/version-mismatch`）被 `emit` 派发 → 挂总线即 `EventModeError`；把按模式派发下沉为 `EventBus.dispatch()`，compare/clarify/quotes 统一走它，并给 AC-COMPARE-001/AC-EVT-001 补断言（原来那条 AC 没挂总线，所以漏了） | `qa ac AC-RFQ-004`（10 断言）+ 强化后的 AC-COMPARE-001（6）与 AC-EVT-001（10）；**46 条 AC 与 s1..s4 全绿**（EV-046） | **B9/T-206+T-207**（护栏扩展 + 产能日历） |
| 2026-09-21 | R18 | B9/T-206+T-207：`services/capacity.py`（产能日历 + 关键路径可行性 + `firm` 交期有效期内模型不可改：人可改/过期可改/indicative 可改；冲突只提请人工、不改交期不否决；对外形态不带私域数值）；`guard` 条款冲突扩到付款/质保/罚则三族 + 产能风险双来源；新增 AC-GUARD-002（6 断言）与 AC-CAP-001（10 断言）；事件 capacity/committed|firm-change-refused|conflict；`04` 补 ctx.capacity。**自查抓到自己的回归**：编辑 `qa/__init__.py` 时漏掉 `checks_cost` → 静默注销 2 条 AC（46→44）、P0 从 34 变 32，是上一批新增的 `verify.sh ac-registry`/`p0-no-node` 报的红（已修，并把教训写进 pitfalls） | `qa ac AC-GUARD-002/AC-CAP-001` 全绿；**48 条 AC 与 s1..s4 全绿**（EV-047） | **B10/T-208**（审计包导出/验证 + 模型输入重建） |
| 2026-09-21 | R19 | B10/T-208 审计包：`evidence.export` 带 HMAC 签名与签名者、`manifest_hash` 绑定清单、`pack_hash` 与 `manifest_hash` 的先后顺序（先 pack 后 manifest，否则互相矛盾）；`canon` 增 `merkle_proof`/`merkle_verify`；新增 `tools/audit-verify.py`（不接触原账本的独立入口，退出码 0/1/2，`--require-signature` 下无签名/无密钥判失败）；新增 FR-EVIDENCE-005 + AC-AUDIT-004（12 断言）；`verify.sh audit` 入口。踩坑：写 checks 时又嵌了 ASCII 双引号（第 4 次）、清理导入时漏掉 `LedgerFrozenError`（单跑才暴露） | `qa ac AC-AUDIT-004`（12）+ AC-AUDIT-001（10）/002（9）回归；**49 条 AC 与 s1..s4 全绿**（EV-048） | **B11/T-210**（人工门队列 + 超时，AC-APPROVE-003） |
| 2026-09-21 | R20 | B11/T-210 人工门队列与超时：`approval.request` 增加超时策略（三选一，非法值直接拒）/摘要/置信度/Flag；`queue_view()`（FR-UX-001 四要素 + 等待时长 + 是否过期）；`sweep()` 按策略动作 （remind 仍待批 / escalate 转 human 上级并继续等待 / abort 作废），**全程 `approval/granted` 为空**；待批不阻塞（只有该动作被 require 挡下，其他请求/落账/比价照常）；重复扫描幂等；事件 approval/reminded|escalated|aborted 登记；`04` 补 ctx.approval 的 P1 | `qa ac AC-APPROVE-003`（11 断言）+ P0 的 001/002 回归；**50 条 AC 与 s1..s4 全绿**（EV-049） | **B12/T-211**（条款库与冲突标注，AC-TERMS-001） |
| 2026-09-21 | R21 | B12/T-211 条款库：新增 `services/terms.py`（版本化 define/revise、`library(as_of)`、`apply_defaults` 只补缺失键并标 `library-default`、`conflicts()` 四类状态、`escalate()` 送人工门、基线仅 human:/bundle: 可写）；`guard` 的条款差异计算下沉到本服务（单一实现）；事件 terms/defined|applied|conflict 登记；AC-TERMS-001（13 断言）。**修真实缺陷两处**：① 分类顺序（先判等值再判单侧缺失最后判未知键，原顺序把等值键误报成冲突 → 护栏 Flag 6→9）；② 交换协议族名别名 `payment_terms_offered` 需在库内单点识别；pitfalls 预算 98% → 按标准顺序合并重复条目 并拆出 `references/pitfalls-evidence.md` | `qa ac AC-TERMS-001`（13）+ AC-GUARD-002（6）/AC-CAP-001（10）回归；**51 条 AC 与 s1..s4 全绿**（EV-050） | **B13/T-212**（变更闭环与生效版本） |
| 2026-09-21 | R22 | B13/T-212 变更闭环：新增 `services/change.py`（propose 引用校验 + 五类拒绝码、recompute 可重放且基准过期报错、approve 经人工门且仅 human:*、effective_total 只计已批准变更、幂等）；事件 change/proposed|priced|approved|rejected 登记；04 新增 `ctx.change`、01 服务清单补一行；字段口径 `basis_unit_price_refs` 恒为列表（单行单元素）+ `basis_unit_price_ref` 首元素。踩坑：① 生成脚本里留下 `+item[...]`（字符串一元加）→ TypeError；② `approvals.decide()` 真实签名是 `by=/decision=` 而非 `approved_by=/granted=`；③ **先写 checklist 的 EV-051 再创建证据文件 → ID 门报未解析**，顺序应为 先落 EV 文件再回填清单 | `qa ac AC-CHANGE-001`（5）+ `AC-CHANGE-002`（8）+ AC-APPROVE-002 / AC-RFQ-004 回归；**53 条 AC 与 s1..s4 全绿**（EV-051） | **B14/T-213**（授标与 PO 闭环） |

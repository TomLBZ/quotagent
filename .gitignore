# 运行时产物（实现期起生效）
# 注意：`lib/` 只忽略**仓库根**的 lib/ —— 写成 `lib/` 会连 `host/lib/` 一起吞掉（实测：
# host/lib/*.mjs 全部不在版本控制里，fresh clone 直接失去宿主库层）。
.venv/
__pycache__/
*.py[cod]
node_modules/
dist/
/lib/
coverage/
tmp/

# 本地状态与密钥：绝不入库
.env
*.local.yaml
config.local.*
.agents/sessions/*.log

# 证据目录保留结构；只排除大体积原始产物
docs/work/evidence/**/*.jsonl.gz
docs/work/evidence/**/tmp/

# 本机用户手册：逐批 goal 指令表（每台机器自备，不入库）
USER-GOALS.md

# 用户空间插件（运行时产物：独立 instance / 独立文件根 / 独立凭据作用域）
user-space/

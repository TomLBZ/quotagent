{
  "name": "rfq-deadline",
  "version": "1.0.0",
  "layer": "domain",
  "provides": [
    "rfq-deadline"
  ],
  "entry": "code/index.mjs",
  "description": "domain 插件「rfq-deadline」：本批 `EV-178` 补**真实承载** —— 入口 `code/index.mjs`（薄包装：`export *` 活绑定 + 透传自述面）+ 实体 `code/rfq-deadline.mjs`（`entry` 指向的入口**真实存在**，`tools/plugin.sh list` 不再报 `artifact-missing`）。**不新造功能**：语义仍只在实体里。",
  "permissions": {
    "writes": ["own-dir", "request-file"],
    "ledger": "sole-writer"
  },
  "migration": {
    "stage": 5,
    "mode": "entity-moved-and-wrapper",
    "impl": "src/domain/rfq-deadline/code/rfq-deadline.mjs",
    "batch": "EV-178"
  }
}

{
  "name": "compare",
  "version": "1.0.0",
  "layer": "domain",
  "provides": [
    "compare"
  ],
  "entry": "code/index.mjs",
  "description": "domain 插件「compare」：迁移阶段 4.2 的**清单先行**形态 —— 需求文档已归位 src/domain/compare/requirements/README.md；`provides` 是占位服务键（真实服务面随 `code/` 落地时改写），`entry` 指向的标准入口此刻**还不存在** ⇒ `tools/plugin.sh list` 如实报 `artifact-missing` 降级，绝不假装已实现。"
}

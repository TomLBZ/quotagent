{
  "name": "faq",
  "version": "1.0.0",
  "layer": "domain",
  "provides": [
    "faq"
  ],
  "entry": "code/__init__.py",
  "description": "domain 插件「faq」：本批 `EV-178` 补**真实承载** —— Python 承载入口 `code/__init__.py`（`from quotagent.services.faq import *` 的原样重导出） + 实体 `code/faq.py`（`entry` 指向的入口**真实存在**，`tools/plugin.sh list` 不再报 `artifact-missing`）。**不新造功能**：语义仍只在实体里。",
  "migration": {
    "stage": 5,
    "mode": "python-entry-wrapper",
    "impl": "src/domain/faq/code/faq.py",
    "batch": "EV-178"
  }
}

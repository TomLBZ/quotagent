{
  "name": "measures",
  "version": "1.0.0",
  "layer": "system",
  "provides": [
    "measures"
  ],
  "entry": "code/index.mjs",
  "description": "system 插件「measures」：迁移阶段 4.2 的**清单先行**形态 —— 需求文档已归位 src/system/measures/requirements/README.md；`provides` 是占位服务键（真实服务面随 `code/` 落地时改写），`entry` 指向的标准入口此刻**还不存在** ⇒ `tools/plugin.sh list` 如实报 `artifact-missing` 降级，绝不假装已实现。"
}

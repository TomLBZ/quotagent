{
  "name": "ui-feedback",
  "version": "1.0.0",
  "layer": "system",
  "provides": [
    "ui-feedback"
  ],
  "entry": "code/index.mjs",
  "description": "system 插件「ui-feedback」：本批 `EV-178` 补**真实承载** —— 入口 `code/index.mjs`（薄包装：`export *` 活绑定 + 透传自述面）+ 实体 `code/ui-feedback.mjs`（`entry` 指向的入口**真实存在**，`tools/plugin.sh list` 不再报 `artifact-missing`）。**不新造功能**：语义仍只在实体里。",
  "migration": {
    "stage": 5,
    "mode": "entity-moved-and-wrapper",
    "impl": "src/system/ui-feedback/code/ui-feedback.mjs",
    "batch": "EV-178"
  }
}

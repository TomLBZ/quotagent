{
  "name": "agent-runtime",
  "version": "1.0.0",
  "layer": "system",
  "provides": [
    "agent-runtime"
  ],
  "entry": "code/index.mjs",
  "description": "system 插件「agent-runtime」：迁移阶段 4.2 的**清单先行**形态 —— 需求文档已归位 src/system/agent-runtime/requirements/README.md；`provides` 是占位服务键（真实服务面随 `code/` 落地时改写），`entry` 指向的标准入口此刻**还不存在** ⇒ `tools/plugin.sh list` 如实报 `artifact-missing` 降级，绝不假装已实现。"
}

{
  "name": "webui",
  "version": "1.0.0",
  "layer": "system",
  "provides": [
    "webui",
    "uiSlots"
  ],
  "entry": "code/index.mjs",
  "description": "system 插件「webui」：双方视角 WebUI —— 把账本投影装配成人能用的页面与只读路由，并提供注入式 UI 注册面（0 业务语义）。本批（接上 `entry`）：`entry` = `code/index.mjs`，只重导出既有实体 `code/webui.mjs`（`provides=['webui','uiSlots']`）⇒ 合法清单（`valid:true`），`provides` 改写为实体自述的真实服务键。",
  "migration": {
    "stage": 4.2,
    "mode": "entity-moved-and-wrapper",
    "impl": "src/system/webui/code/webui.mjs",
    "from": "host/modules/webui.mjs",
    "moves_in_stage": 4.2
  }
}
